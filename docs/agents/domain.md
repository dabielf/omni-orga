# Domain docs

Before changing code, read:

- `CONTEXT.md`
- Any useful decision records in `docs/adr/`

If these files do not exist, continue without asking to create them.

This is a single-context project:

```
/
├── CONTEXT.md
├── docs/adr/
└── src/
```

Use the terms defined in `CONTEXT.md`.

If a change conflicts with a decision record, state the conflict.
